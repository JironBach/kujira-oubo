<%= form_with(model: applicant_display, local: true) do |form| %>
  <% if applicant_display.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(applicant_display.errors.count, "error") %> prohibited this applicant_display from being saved:</h2>

      <ul>
      <% applicant_display.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
